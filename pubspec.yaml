name: gestion_incidentes_ti
description: "Aplicación móvil de gestión de incidentes TI - Proyecto Integrador Etapa 3"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  cloud_firestore: ^4.15.5
  sqflite: ^2.3.0
  path_provider: ^2.1.1

flutter:
  uses-material-design: true
